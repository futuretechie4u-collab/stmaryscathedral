{
  "name": "stmaryscathedral",
  "private": true,
  "scripts": {
    "build": "npm install --prefix tnp-proj && npm run build --prefix tnp-proj",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
